To run the desktop assistant, you need to download certain libraries from pip(python package installer)
1. pyttsx3 (pip install pyttsx3)
2. speechRecognition
3. smptlib
4. os
5. datetime
6. webbrowser
To install any library using pip the syntax must be
pip install "name of the library"
